import { Overpass, Inter } from "next/font/google";
export const inter = Inter({ subsets: ["latin"] ,display: 'swap', variable: '--font-inter'});

export const overpass = Overpass({ subsets: ["latin"] ,display: 'swap', variable: '--font-overpass'});
